package com.salkcoding.oswl.repository;

/**
 * @deprecated Replaced by {@link ScanComponentRepository} and {@link LibraryRepository}.
 *             This interface is retained only to prevent compilation failures in classes
 *             that may still import it. Remove after all references have been migrated.
 */
@Deprecated(forRemoval = true)
public interface ComponentRepository {
}
