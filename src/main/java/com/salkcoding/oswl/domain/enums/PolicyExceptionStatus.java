package com.salkcoding.oswl.domain.enums;

/**
 * Lifecycle state of a policy-exception (waiver) request.
 */
public enum PolicyExceptionStatus {
    PENDING,
    APPROVED,
    REVOKED,
    EXPIRED
}
