package com.salkcoding.oswl.dto.search;

/** Project hit for the global search palette — links to the project's security center. */
public record ProjectSearchHit(
        Long id,
        String name
) {
}
